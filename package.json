{
  "name": "donera",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "types": "turbo types",
    "lint": "turbo lint",
    "test": "turbo test",
    "fmt": "prettier --write .",
    "devnet:up": "cd vendor/alephium-stack && make start-devnet",
    "devnet:down": "cd vendor/alephium-stack && make stop-devnet"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "turbo": "^1.12.4"
  },
  "pnpm": {
    "overrides": {
      "sharp": "^0.33.2"
    }
  }
}
